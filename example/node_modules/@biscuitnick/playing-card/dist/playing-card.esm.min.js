customElements.define("playing-card",class extends HTMLElement{constructor(){self=super(),self.card=self.getAttribute("card");const e=document.createElement("template");e.innerHTML=`<div>\n      ${self.card}\n      </div>`;const t=e.content;self.attachShadow({mode:"open"}).appendChild(t.cloneNode(!0))}});
